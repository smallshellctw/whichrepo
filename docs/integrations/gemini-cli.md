# Gemini CLI

Add WhichRepo to the `mcpServers` object in Gemini CLI settings:

```json
{
  "mcpServers": {
    "whichrepo": {
      "command": "whichrepo",
      "args": ["mcp", "--workspace", "/path/to/workspace"]
    }
  }
}
```
