# OpenCode

Add a local MCP entry to `opencode.json`:

```json
{
  "mcp": {
    "whichrepo": {
      "type": "local",
      "command": ["whichrepo", "mcp", "--workspace", "/path/to/workspace"]
    }
  }
}
```
